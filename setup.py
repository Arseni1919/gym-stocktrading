from setuptools import setup

setup(name='gym_stocktrading',
      version='0.0.1',
      install_requires=['gym', 'pandas', 'numpy', 'matplotlib']  # And any other dependencies gym_stocktrading needs
      )